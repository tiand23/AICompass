# Model Efficiency

## 概要

モデルの効率化と小型化：より少ない計算資源で使えるインテリジェンスを引き出す——小規模言語モデル（SLM）、知識蒸留、低ビット量子化、効率的アーキテクチャ（Liquid AI の非 Transformer 系 LFM など）、CPU/エッジ推論。

## なぜ重要か

フロンティアモデルは巨大化し続けるが、本番ワークロードの多く（分類、埋め込み、抽出、ルーティング）にフロンティア級の知能は不要——必要なのは**十分・安価・高速**。効率化技術は AI アプリのユニットエコノミクスとデプロイ境界（CPU・エッジ・オンプレで動くか）を決める、「生成 AI の大規模実装」の見えない土台。

## コアコンセプト

- **SLM**：数億〜数十億パラメータの専用モデル。蒸留と組み合わせれば狭いタスクで大型モデルの品質に迫る。
- **知識蒸留**：大型モデル（教師）の出力で小型モデル（生徒）を訓練し、能力を小さな器に圧縮する。
- **低ビット量子化**：4-bit などの精度圧縮で VRAM とレイテンシを大幅削減（拡散モデル側の Nunchaku を参照）。
- **効率的アーキテクチャ**：Liquid AI の LFM など非 Transformer 路線。同等品質で桁違いに低い推論コストを狙う。
- **CPU/エッジ推論**：GPU に依存しないデプロイ形態——embedding・エンコーダ系負荷では既に実用段階。
- **階層ロード推論**：重みをレイヤーごとにロードし時間で VRAM を買う（airllm：4GB GPU で 70B）。品質は落ちないが速度を犠牲に——低頻度オフラインタスク向け、量子化/蒸留と相互補完。
- **ローカル Agentic マルチモーダルオープンモデル**：新興のモデル群（Meta Muse Glimmer など）は「ローカル実行可能・Agentic・マルチモーダル・オープンソース」を同時に満たし、ゲート付き GQA などのメモリ最適化技術でパラメータ数をコンシューマー向けハードウェアが許容できる範囲に抑える——単なる小型分類/埋め込みモデルではない。
- **実行時モデルルーティング**：小型モデルを訓練するのではなく、Agent の実行時に呼び出しを安価/高価なモデル間で動的に振り分ける——LangChain の NVIDIA Switchyard 実測では、Deep Agents 評価セットの呼び出しの 93% が 30B モデルにルーティングされ、前沿モデルが必要なのはわずか 7%、コストは 74% 削減された。割に合うかどうかは判断モデルのコストと 2 階層のモデルの価格差の比率次第——価格差が大きいほどルーティングは割に合う。
- **極小体積の専門特化モデル**：汎用的な対話品質を追わず、ツール呼び出し/構造化抽出のような狭いタスクに特化し、積極的な量子化で体積を極限まで圧縮する（needle：14MB 単一ファイル、実行時メモリ約 28MB、スマホ/ウェアラブル/ロボット向け）——SLM 路線の体積軸における極端なサンプルで、「十分であればよい」という効率哲学と一貫する。
- **ローカル訓練/推論がコマンドラインから GUI へ**：Unsloth Desktop は、これまでスクリプト駆動だったローカルファインチューニング（LLM と画像/動画拡散モデルの LoRA）をデスクトップ GUI にまとめ、`unsloth start` で Claude Code/Codex などのコーディング Agent に直結する——ローカル効率化ツールの使いやすさの敷居が下がりつつあり、効率化数値そのものだけが進化しているのではない（詳細は [coding-agents](/ja/topics/coding-agents)）。

## 関連技術

- [diffusion-models](/ja/topics/diffusion-models)（画像側の量子化推論）
- [rag](/ja/topics/rag)（embedding 工程は効率化技術の直接の受益者）
- [claude-models](/ja/topics/claude-models) / [gemini-models](/ja/topics/gemini-models)（大型モデル側のトークン効率も同じ命題）

## ベストプラクティス

- モデルを選ぶ前に「この工程にどれだけの知能が必要か」を問う——ルーティング・抽出・埋め込みは SLM/エンコーダで、大型モデルの予算は本当に難しい推論に残す。
- 量子化・小型化後は必ず自前の評価セットで回帰テストする。品質劣化はタスク依存。

## 推奨学習リソース

- [LFM2.5-Encoders: Fast Long-Context Inference on CPU（HuggingFace Blog）](https://huggingface.co/blog)

## Timeline

### [2026-08-12](/ja/today/2026-08-12)

Unsloth がデスクトップアプリ（beta）を公開：ローカルモデルの訓練と推論を GUI に統合、`unsloth start` で Claude Code/Codex に直結；公式データでは訓練速度 2 倍、VRAM 70% 削減（詳細は [coding-agents](/ja/topics/coding-agents)）。同日 needle 2 が登場：14MB の単一ファイル基盤モデルで、ツール呼び出しと構造化抽出に特化し、スマホ/ウェアラブル/ロボット向け、同クラスの小型モデルより 5〜70 倍小さい。

### [2026-08-11](/ja/today/2026-08-11)

LangChain が NVIDIA NeMo Switchyard で自社の Deep Agents 評価セットを実測：93% の呼び出しが 30B 級の Nemotron 3.5 Lightning にルーティングされ（費用の 10.4% のみ消費）、7% が Claude Opus 4.8 にエスカレート（費用の 68.4% を消費）、Opus 単独実行に対して全体コストを 74% 削減、精度低下はわずか 6 ポイント。ルーティングが割に合うかどうかを判断する式（判断モデルのコスト vs 2 階層の価格差）も提示（詳細は [deep-agents](/ja/topics/deep-agents)）。

### [2026-08-10](/ja/today/2026-08-10)

Multiverse Computing が知識蒸留を大規模に低コストで行う新手法を発表——大型モデルの能力を小型モデルに圧縮する計算コストの壁をさらに下げ、ベンダー事前訓練の SLM に頼らず自前の専用蒸留モデルを構築する選択肢をより多くのチームに開く。同日、Meta が Muse Glimmer を発表：300 億パラメータ（20 億視覚エンコーダ + 280 億テキストデコーダ）のローカル/Agentic/マルチモーダル、Apache 2.0 オープンソースモデルで、ゲート付き GQA によりメモリ要件を 16 倍削減、発表と同時に transformers/llama.cpp/vLLM など主要推論エコシステムをサポート。

### [2026-08-04](/ja/today/2026-08-04)

Liquid AI が LFM2.5-2.6B を発表（「Deploy local agents everywhere」）：SLM が分類/抽出の脇役ではなく、エッジのローカルエージェントの推論主体を担い始めた——非 Transformer 高効率アーキテクチャ路線が実装され続ける。

### [2026-08-03](/ja/today/2026-08-03)

airllm が Trending 再登場（日次 +819）：階層ロードで 70B モデルを 1 枚の 4GB GPU で実行——ローエンドハードウェアで大型モデルを動かす需要が継続。

### [2026-07-28](/ja/today/2026-07-28)

Liquid AI が LFM2.5-Encoders を発表：CPU 上の高速長文脈推論エンコーダ——埋め込み/検索系負荷の GPU 離れが実用段階に入ったシグナル。
