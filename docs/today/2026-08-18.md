# 2026-08-18

## 社区发布 817 条网络安全 Skill：映射 6 大安全框架，兼容 Claude Code 等 20+ 平台

- **一句话总结**：mukul975/Anthropic-Cybersecurity-Skills（单日 +198，累计 2.85 万 star）——社区维护的网络安全 Skill 库，817 条来自真实从业者工作流（而非通用教程或脚本）的结构化技能，覆盖 29 个安全领域（云安全 66 条、威胁狩猎 58 条、威胁情报 52 条、网络安全 43 条、Web 应用安全 42 条、数字取证 41 条、恶意软件分析 39 条、身份与访问管理 37 条等，直至合规与硬件固件安全）。每条 skill 统一采用 YAML frontmatter（供 Agent 快速发现）+ Markdown 分步流程 + 参考文档 + 辅助脚本 + 模板资产的结构，并映射到六大行业框架：MITRE ATT&CK v19.1（805 条技能对应 15 个战术、290 项技术）、NIST CSF 2.0（804 条覆盖全部六大职能）、MITRE ATLAS 2026.07（93 条，专门针对 AI/ML 对抗威胁与 Agentic AI 攻击向量）、MITRE D3FEND v1.4.0（139 条防御对抗措施）、NIST AI RMF 1.0（97 条治理与 GenAI 风险类别）、MITRE F3 反欺诈框架（94 条网络金融欺诈对抗技能）。为控制 token 消耗采用渐进式披露：仅扫描 frontmatter 每条约 30 token（Agent 可一次性搜完全部 817 条），命中候选才加载完整内容（500-2000 token）。原生兼容 Claude Code、GitHub Copilot、Cursor、Gemini CLI、OpenAI Codex CLI 等 20+ 遵循 agentskills.io 标准的平台，Apache 2.0 协议。
- **为什么重要**：这是网络安全这个高专业度垂直领域第一次出现体量如此之大、且系统性映射到六大行业标准框架（而非仅 MITRE ATT&CK 一家）的开源 skill 集合——把网络安全从业者的隐性经验（威胁狩猎怎么做、数字取证的步骤是什么）结构化成 Agent 可直接加载执行的知识层，与知识库此前记录的 OpenAI Daybreak（网络安全模型分级准入）、reverse-skill（渗透测试 skill 路由包）等信号放在一起看，"网络安全 Agent 化"正在从模型层（专用模型）、工具层（渗透测试工具）向知识层（结构化技能库）全面铺开。"frontmatter 轻扫描+按需加载完整内容"这一渐进式披露设计，也是大规模 skill 库控制上下文成本的一个可复用的具体范式。
- **分类**：Agent
- **关联 Topic**：[agent-skills](/topics/agent-skills) ・ [agentic-safety](/topics/agentic-safety)
- **来源**：https://github.com/mukul975/Anthropic-Cybersecurity-Skills

## ai-memory 开源：编码 Agent 的长期记忆层，支持跨厂商无缝交接任务

- **一句话总结**：akitaonrails/ai-memory（单日 +207，累计约 2.1k star，MIT 协议）——面向编码 Agent CLI 的长期记忆系统，核心场景是"在 Claude Code 里中途退出任务，换到同目录下的 OpenAI Codex 继续，不需要重新给它讲一遍架构"。不存储原始对话日志，而是把会话生命周期事件（prompt、工具调用、失败记录）脱敏后提炼编译成可检索的 Markdown wiki；会话结束时生成包含"未解决问题、下一步计划"的交接摘要，下一个 Agent 在收到第一条 prompt 前会先拿到"上次进展到哪"的总结。架构上是单一 Rust 二进制管理一个数据目录（wiki 源真相/git 版本化、raw 脱敏转录、SQLite+FTS5 数据库、本地 embedding 模型、滚动日志），生命周期 hook 把事件"发后不理"式 POST 给本地 HTTP 服务器，服务器串行化写入 SQLite，编译会话页面，并通过 FTS5 全文检索+实体匹配 RRF+可选向量检索提供召回。已为 Claude Code、Codex、Command Code、Devin、OpenCode、Cursor、Kiro CLI 等实现了各自的 hook 适配——不同 Agent 的 hook schema 不同，但都把观察数据 POST 到同一个服务器，这是跨厂商交接得以实现的关键。另提供 MCP server（`memory_query`/`memory_write_page`/`memory_feedback` 等工具）、只读 Web UI，以及不依赖任何 LLM 的"零 LLM 模式"（退化为 FTS5+声明式实体+规则摘要）。
- **为什么重要**：此前记忆基础设施的讨论大多聚焦"同一个 Agent/同一个厂商内怎么记得住"，ai-memory 把范围扩大到"换一个厂商的 Agent 继续干活，记忆能不能带得走"——这是编码 Agent 生态"多工具并用成为常态"（此前 holaOS 也体现了同一趋势）之后一个自然但此前少有人做的下一步：记忆本身不应该被锁定在单一供应商的 harness 里。"零 LLM 模式"这个设计选择也值得注意——检索与摘要的基础能力不依赖任何模型调用，把"记忆系统本身"和"用不用得起大模型"解耦，是一种务实的降低使用门槛的思路。
- **分类**：Agent
- **关联 Topic**：[agent-memory](/topics/agent-memory) ・ [coding-agents](/topics/coding-agents)
- **来源**：https://github.com/akitaonrails/AI-Memory
