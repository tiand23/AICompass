# AI Compass V1 实施计划与说明

> **定位：给自己用的 AI 开发知识库，手动维护，好用了再考虑公开。**
>
> 本文档是 V1 的实际执行依据。原《项目设计文档（PRD + Architecture）》保留作为长期愿景参考，两者冲突时以本文档为准。

------------------------------------------------------------------------

# 一、V1 的边界

## 做什么

只做两件事：

1. **`/update`**：手动执行，抓取上次运行以来的更新，写入 Daily，同步更新 Topic。
2. **Topic 体系**：每个知识点一个 Markdown 文件，长期积累。

## 不做什么（公开后再说）

| 砍掉的功能 | 原因 | 替代方案 |
|---|---|---|
| Weekly / Monthly 定期文件 | 自用不需要预先固化 | 想回顾时让 Claude Code 现场读 `docs/today/` 即时生成 |
| ~~GitHub Pages~~ | ~~自己在编辑器 / GitHub 里看 Markdown 就够了~~ **2026-07-28 变更：改为 VitePress + Cloudflare Pages，push 后自动构建部署** | 见下方"发布"一节 |
| 预设知识树 | 拍脑袋的分类没有意义 | 跑一两个月后从 `topics/` 的实际内容里归纳 |
| 推荐指数 / 是否值得学习 | 值不值得学得自己判断，AI 打分没有参考价值 | Daily 里只写客观信息（是什么、为什么重要），判断留给自己 |
| 自动化（GitHub Actions 等） | 手动执行足够，且避免 API key / 成本 / 重试等一堆工程问题 | 每天（或想起来时）手动跑 `/update` |

------------------------------------------------------------------------

# 二、核心设计：`/update` 的补漏逻辑

**手动执行意味着一定会漏天**（周末、出差、忘了）。所以 `/update` 的语义不是"生成今天的日报"，而是：

> **补齐从上次运行到现在的所有内容。**

执行流程：

1. 查看 `docs/today/` 中最新文件的日期，作为起点（首次运行则只抓当天）。
2. 抓取各数据源从起点到今天的更新。
3. 每个自然日生成一个 `docs/today/YYYY-MM-DD.md`（当天没有值得记录的内容就不生成，不写空文件）。
4. 对每条值得记录的内容，判断是否需要新建或更新对应 Topic。
5. Git commit（一次运行一个 commit，message 注明覆盖的日期范围）。

这样系统对"手动"这件事免疫：隔三天跑一次和每天跑一次，最终积累的内容一样。

------------------------------------------------------------------------

# 三、目录结构

``` text
AICompass/
├── docs/
│   ├── .vitepress/     # VitePress 站点配置（侧边栏自动生成）
│   ├── index.md        # 站点首页
│   ├── today/          # YYYY-MM-DD.md，每日更新
│   └── topics/         # 每个知识点一个文件
├── profile.md          # （可选）我的方向和关注点，用于过滤无关信息
├── .claude/skills/     # /update /topic /cleanup
├── package.json        # VitePress 依赖
└── README.md
```

## 发布（2026-07-28 新增）

- 站点用 **VitePress** 构建（`npm run docs:build`），**Cloudflare Pages** 绑定 GitHub 仓库，每次 push 自动构建部署。
- `/update` 跑完自动 commit + push，所以网站每天自动更新，无需额外操作。
- Topic 间引用与 Daily 的关联 Topic 一律用标准 Markdown 链接 `[name](/topics/name)`，不用 `[[双链]]`。

比原 PRD 少了 weekly/、monthly/、assets/、prompts/、scripts/、.github/workflows/ —— 需要时再加，先不建空目录。

------------------------------------------------------------------------

# 四、数据来源（V1）

**官方渠道：**

- OpenAI Blog
- Anthropic（Blog / Release Notes）
- Google AI
- Microsoft AI
- LangChain Blog
- LlamaIndex Blog

**社区聚合（非官方，用于捕捉生态动向）：**

- GitHub Trending
- HuggingFace

> 注：原 PRD 写"仅官方"但列表混入了社区源，此处明确区分两类。V2 再考虑 Reddit / HN / X / arXiv。
>
> 如果单次运行耗时过长，优先保证官方渠道，社区源可降频（比如每周扫一次）。

------------------------------------------------------------------------

# 五、Daily 模板

`docs/today/YYYY-MM-DD.md`：

``` markdown
# 2026-07-27

## <条目标题>

- **一句话总结**：
- **为什么重要**：
- **分类**：Models / Agent / Coding / RAG / ...
- **关联 Topic**：[mcp](/topics/mcp) [claude-code](/topics/claude-code)
- **来源**：<链接>
```

不含推荐指数、不含"是否值得学习"——判断留给自己。AI 的职责是把事实和上下文讲清楚。

（可选）如果建了 `profile.md`，`/update` 会用它过滤明显无关的内容（比如不关注语音方向就不收语音类更新），但**只做过滤，不做打分**。

------------------------------------------------------------------------

# 六、Topic 模板与维护规则

`docs/topics/<name>.md`：

``` markdown
# <Topic 名称>

## 简介

## 为什么重要

## 核心概念

## 相关技术

## 最佳实践

## 推荐学习资料

## Timeline

### 2026-07-27
...
```

## 维护规则（更新时必须遵守）

1. **不直接追加**：先判断新内容是否已被现有内容覆盖，重复则跳过，相似则合并。
2. **Timeline 按日期倒序**，新条目在上。
3. **归档规则**：Topic 文件超过约 500 行时，Timeline 只保留最近 3 个月的条目，更早的压缩为"历史演进摘要"一段放在 Timeline 之前。避免文件无限膨胀、超出更新时的上下文容量。

------------------------------------------------------------------------

# 七、Skills 设计

### /update（核心，第一个实现）

- 补漏逻辑见第二章。
- 一次运行完成：抓取 → 生成 Daily → 更新 Topic → commit。

### /topic `<名称>`

- 不存在则按模板创建（AI 先调研该主题的背景，填充简介/核心概念等）。
- 存在则按第六章规则更新。

### /cleanup（低频，手动触发）

- 全库去重、合并相似 Topic、执行 Timeline 归档、修复失效的 Topic 间引用。
- 建议每月跑一次，或发现文档变乱时跑。

### /weekly、/monthly

- **不落盘、不预实现**。需要回顾时直接对 Claude Code 说"总结一下最近两周的 daily"即可。等公开后如果需要固定产出，再做成 skill。

------------------------------------------------------------------------

# 八、实施计划

| 步骤 | 内容 | 完成标准 |
|---|---|---|
| 1 | 初始化仓库：目录结构、README、Daily / Topic 模板 | 结构就位，git init 完成 |
| 2 | 实现 `/update` skill（含补漏逻辑） | 手动跑一次能产出当天 Daily 并正确创建 / 更新 Topic |
| 3 | 试运行一周：每天手动跑，观察产出质量 | Daily 内容值得读、Topic 更新不重复不跑偏 |
| 4 | 根据试运行调整 prompt（信息密度、过滤标准、Topic 粒度） | 连续几天产出不需要手工修正 |
| 5 | 实现 `/topic` 和 `/cleanup` | 能手动补建 Topic、能整理全库 |
| 6 | （可选）建 `profile.md`，收敛信息范围 | 无关内容明显减少 |
| 7 | 稳定运行一个月后评估：好用吗？ | 好用 → 考虑公开（补 Pages / Weekly 等）；不好用 → 调整或止损 |

------------------------------------------------------------------------

# 九、公开后再做（Backlog）

按原 PRD 的愿景保留，V1 一概不碰：

- GitHub Pages 展示层
- Weekly / Monthly 固定产出
- 知识树导航（从实际 Topic 中归纳）
- 全文搜索、RSS 输出
- 社区数据源（Reddit / HN / X / YouTube / arXiv）
- MCP Server、向量检索、Chat with AI Compass
